{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window only needs Tauri core IPC for the reader event channel.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
